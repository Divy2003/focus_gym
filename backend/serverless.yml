org: divypatel
app: focus-api
service: focus-gym-api

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  timeout: 60  # Increased for PDF generation
  memorySize: 1024  # Increased for Chromium
  httpApi:
    payload: '2.0'
    cors: false
  environment:
    MONGODB_URI: ${env:MONGODB_URI}
    JWT_SECRET: ${env:JWT_SECRET}
    MSG91_API_KEY: ${env:MSG91_API_KEY}
    MSG91_TEMPLATE_ID: ${env:MSG91_TEMPLATE_ID}
    MSG91_SENDER_ID: ${env:MSG91_SENDER_ID}
    ADMIN_MOBILE: ${env:ADMIN_MOBILE}
    CLOUDINARY_CLOUD_NAME: ${env:CLOUDINARY_CLOUD_NAME}
    CLOUDINARY_API_KEY: ${env:CLOUDINARY_API_KEY}
    CLOUDINARY_API_SECRET: ${env:CLOUDINARY_API_SECRET}
    NODE_ENV: production

functions:
  api:
    handler: server.handler
    layers:
      # Chromium Lambda Layer for PDF generation
      - arn:aws:lambda:us-east-1:764866452798:layer:chrome-aws-lambda:50
    events:
      - httpApi:
          path: /{proxy+}
          method: ANY
      - httpApi:
          path: /
          method: ANY

plugins:
  - serverless-dotenv-plugin

package:
  patterns:
    - '!node_modules/.cache/**'
    - '!.git/**'
    - '!.gitignore'
    - '!README.md'
    - '!.env.example'
    - '!tests/**'
    - '!.vscode/**'
    - '!node_modules/aws-sdk/**'
